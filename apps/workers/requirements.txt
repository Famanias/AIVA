fastapi==0.115.0
uvicorn[standard]==0.30.0
httpx==0.27.2
pydantic>=2.11.7,<3.0.0
pydantic-settings==2.4.0

# Async utilities
anyio==4.6.0

# LLM providers
google-generativeai==0.8.0
groq==0.11.0
openai==1.51.0  # used for OpenRouter compatibility

# Web search
tavily-python==0.3.9

# TTS — self-hosted integrations are called via subprocess/HTTP
# Cloud fallback
edge-tts>=7.0.0

# Subtitles
faster-whisper==1.0.3

# B-roll matching
sentence-transformers==3.1.1
numpy==1.26.4

# Image processing
Pillow==10.4.0

# Audio processing (loudness normalization)
pyloudnorm==0.1.1
soundfile==0.12.1

# Storage
supabase==2.31.0
boto3==1.35.0  # S3-compatible storage fallback

# Database
asyncpg==0.29.0
cryptography>=42.0.0

# Redis (for cache reads — queue management stays in Node.js/BullMQ)
redis==5.1.0

# Utilities
python-dotenv==1.0.1
structlog==24.4.0

tenacity<9.0.0   # retry with exponential backoff

# Testing
pytest
pytest-asyncio<0.24.0
pytest-httpx